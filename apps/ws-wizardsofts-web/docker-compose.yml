services:
  frontend:
    build: .
    container_name: ws-wizardsofts-web
    restart: always
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.wizardsofts-web.rule=Host(`www.wizardsofts.com`) || Host(`wizardsofts.com`)"
      - "traefik.http.routers.wizardsofts-web.entrypoints=websecure"
      - "traefik.http.routers.wizardsofts-web.tls.certresolver=letsencrypt"
      - "traefik.http.services.wizardsofts-web.loadbalancer.server.port=3000"
    environment:
      - NODE_ENV=production
    expose:
      - "3000"
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://127.0.0.1:3000"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    deploy:
      resources:
        limits:
          cpus: '0.50'
          memory: 1G
    networks:
      - microservices-overlay

networks:
  microservices-overlay:
    external: true
