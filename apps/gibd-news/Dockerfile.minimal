FROM python:3.11-slim
WORKDIR /app
COPY scripts/config_loader.py scripts/
RUN mkdir -p scripts
CMD ["python", "-c", "from scripts.config_loader import get_api_keys; print('✓ Config loader works!'); print(get_api_keys())"]
