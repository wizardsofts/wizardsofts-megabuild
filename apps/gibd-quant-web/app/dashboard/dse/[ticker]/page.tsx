'use client';

/**
 * Default Ticker Page - Placeholder for default route
 *
 * URL: /dashboard/dse/{ticker}
 * Example: /dashboard/dse/BATBC
 *
 * This page is just a placeholder. The layout.tsx handles all rendering
 * based on the URL pathname and activeTab state. This file needs to exist
 * for the route to be valid in Next.js, but returns null to let the layout
 * manage the content rendering.
 */

export default function TickerPage() {
  // Layout handles all content rendering
  return null;
}
