# Environment variables for microservices stack deployment
# Database password for PostgreSQL on Server 80 (10.0.0.80:5435)
DB_PASSWORD=29Dec2#24

# API key for write operations (if not set, write operations will fail)
# Generate a secure random key: openssl rand -hex 32
API_KEY=
