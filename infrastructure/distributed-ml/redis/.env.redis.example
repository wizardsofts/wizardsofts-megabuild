# Redis Configuration for Celery Broker
# Copy this file to .env.redis and update the password

# Redis password (generate with: openssl rand -hex 32)
REDIS_PASSWORD=CHANGE_THIS_TO_SECURE_PASSWORD_RUN_OPENSSL_RAND_HEX_32
